import { uiGroups, syntaxGroups } from '../theme'

const tags = {
  inProgress: {
    label: 'In Progress',
    color: uiGroups.userCurrentState,
    icon: 'timer',
  },

  general: {
    label: 'General',
    color: syntaxGroups.type,
    icon: 'tag',
  },

  hardware: {
    label: 'Hardware',
    color: syntaxGroups.type,
    icon: 'plug',
  },

  terminal: {
    label: 'Terminal',
    color: syntaxGroups.type,
    icon: 'terminal',
  },

  git: {
    label: 'Git',
    color: syntaxGroups.type,
    icon: 'git',
  },

  vim: {
    label: 'Vim',
    color: syntaxGroups.type,
    icon: 'vim',
  },

  typescript: {
    label: 'TypeScript',
    color: syntaxGroups.type,
    icon: 'typescript',
  },

  flow: {
    label: 'Flow',
    color: syntaxGroups.type,
    icon: 'flow',
  },

  graphQl: {
    label: 'GraphQL',
    color: syntaxGroups.type,
    icon: 'graphql',
  },

  node: {
    label: 'Node',
    color: syntaxGroups.type,
    icon: 'node',
  },

  react: {
    label: 'React',
    color: syntaxGroups.type,
    icon: 'react',
  },

  reactNative: {
    label: 'React Native',
    color: syntaxGroups.type,
    icon: 'react',
  },

  electron: {
    label: 'Electron',
    color: syntaxGroups.type,
    icon: 'electron',
  },

  redux: {
    label: 'Redux',
    color: syntaxGroups.type,
    icon: 'redux',
  },

  javascript: {
    label: 'JavaScript',
    color: syntaxGroups.type,
    icon: 'javascript',
  },

  css: {
    label: 'CSS',
    color: syntaxGroups.type,
    icon: 'css',
  },

  go: {
    label: 'Go',
    color: syntaxGroups.type,
    icon: 'go',
  },

  php: {
    label: 'PHP',
    color: syntaxGroups.type,
    icon: 'php',
  },

  ruby: {
    label: 'Ruby',
    color: syntaxGroups.type,
    icon: 'ruby',
  },

  postgresql: {
    label: 'PostgreSQL',
    color: syntaxGroups.type,
    icon: 'postgresql',
  },
}

export default tags
