import { uiGroups, syntaxGroups } from '../theme'

const tags = {
  inProgress: {
    label: 'in-progress',
    color: uiGroups.userCurrentState,
    icon: 'timer',
  },

  terminal: {
    label: 'terminal',
    color: syntaxGroups.type,
    icon: 'terminal',
  },

  unix: {
    label: 'unix',
    color: syntaxGroups.type,
    icon: 'unix',
  },

  vim: {
    label: 'vim',
    color: syntaxGroups.type,
    icon: 'vim',
  },

  git: {
    label: 'git',
    color: syntaxGroups.type,
    icon: 'git',
  },

  npm: {
    label: 'npm',
    color: syntaxGroups.type,
    icon: 'npm',
  },

  react: {
    label: 'react',
    color: syntaxGroups.type,
    icon: 'react',
  },

  node: {
    label: 'node',
    color: syntaxGroups.type,
    icon: 'node',
  },

  graphQl: {
    label: 'graphql',
    color: syntaxGroups.type,
    icon: 'graphql',
  },

  hardware: {
    label: 'hardware',
    color: syntaxGroups.type,
    icon: 'plug',
  },

  general: {
    label: 'general',
    color: syntaxGroups.type,
    icon: 'tag',
  },
}

export default tags
