import { uiGroups } from '../theme'

const tags = {
  inProgress: {
    label: 'in-progress',
    color: uiGroups.userActionNeeded,
    icon: 'timer',
  },

  general: {
    label: 'general',
    color: uiGroups.userCurrentState,
    icon: 'tag',
  },

  unix: {
    label: 'unix',
    color: uiGroups.userCurrentState,
    icon: 'terminal',
  },

  git: {
    label: 'git',
    color: uiGroups.userCurrentState,
    icon: 'git',
  },

  npm: {
    label: 'npm',
    color: uiGroups.userCurrentState,
    icon: 'npm',
  },

  react: {
    label: 'react',
    color: uiGroups.userCurrentState,
    icon: 'react',
  },

  node: {
    label: 'node',
    color: uiGroups.userCurrentState,
    icon: 'node',
  },

  graphQl: {
    label: 'graphql',
    color: uiGroups.userCurrentState,
    icon: 'graphql',
  },

  hardware: {
    label: 'hardware',
    color: uiGroups.userCurrentState,
    icon: 'plug',
  },
}

export default tags
